
@{
    ViewBag.Title = "分类管理";
    Layout = "~/Areas/Manage/Views/Shared/_Layout.cshtml";
    var list = ViewBag.List as List<MVCLite.Models.Catalog>;
}


        <div class="padding border-bottom">
                    <button type="button" class="button border-blue" onclick="window.location.href='/manage/catalog/add'">新增分类</button>

        </div>
        <table class="table table-hover text-center">
            <tr>
                <th width="120">ID</th>
                <th>名称</th>
                <th width="120">时间</th>
                <th>操作</th>
            </tr>
            @for (int i = 0; i < list.Count; i++)
            {
                <tr>
                    <td>
                        @list[i].ID
                    </td>
                    <td>@list[i].Name</td>
                    <td>@list[i].Modifytime.ToString()</td>
                    <td><div class="button-group"> <a class="button border-red" href="javascript:void(0)" onclick="return del(1)"><span class="icon-trash-o"></span> 删除</a> </div></td>
                </tr>
            }

        </table>
<script type="text/javascript">

    function del(id) {
        if (confirm("您确定要删除吗?")) {

        }
    }

    $("#checkall").click(function () {
        $("input[name='id[]']").each(function () {
            if (this.checked) {
                this.checked = false;
            }
            else {
                this.checked = true;
            }
        });
    })

    function DelSelect() {
        var Checkbox = false;
        $("input[name='id[]']").each(function () {
            if (this.checked == true) {
                Checkbox = true;
            }
        });
        if (Checkbox) {
            var t = confirm("您确认要删除选中的内容吗？");
            if (t == false) return false;
        }
        else {
            alert("请选择您要删除的内容!");
            return false;
        }
    }

</script>

